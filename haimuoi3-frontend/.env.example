# Railway / CI build — required for production build
# Include https:// (script adds it if missing, but set explicitly on Railway)
API_URL=https://your-backend.up.railway.app/api

# Local ng serve uses src/environments/environment.ts (localhost)
